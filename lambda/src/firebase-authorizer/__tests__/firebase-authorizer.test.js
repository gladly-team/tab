/* eslint-env jest */

// const firebaseAuthorizer = require('../firebase-authorizer')

test('fails when no token is provided', () => {
  // TODO
  // return firebaseAuthorizer.handler({ queryStringParameters: {} })
  //   .then(response => {
  //     expect(response.statusCode).toBe(401)
  //   })
})
