/* eslint-env jest */

// import callRedis from '../redis'

afterEach(() => {
  jest.clearAllMocks()
})

// TODO
describe('Redis outbound', () => {
  it('is a placeholder', async () => {
    expect.assertions(1)
    expect(true).toBe(true)
  })
})
